import {defineComponent} from "vue";

export default defineComponent({
    components: {},
    data() {
        return {
            test: 'test',
        }
    },
    props: {},
    setup(props, {emit}) {

    },
})